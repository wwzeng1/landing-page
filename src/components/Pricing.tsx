import React from "react";
const Pricing = () => {
  const handleClick = () => {
    // Trigger the Fireworks component
  };

  return (
    <div onClick={handleClick}>
      <h1>Pricing</h1>
    </div>
  );
};

export default Pricing;
